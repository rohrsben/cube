usage looks like

`./code <pattern>`

where pattern is a sequence of possible moves, each its own argument

to get the possible moves, easiest way is to just look at the runner function in main.rs

example:
TODO add example once I set up a rust environment
